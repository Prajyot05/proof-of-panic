[workspace]
members = [
    "programs/proof-of-panic",
    "programs/target-perp",
    "simulator",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.dev]
overflow-checks = true
